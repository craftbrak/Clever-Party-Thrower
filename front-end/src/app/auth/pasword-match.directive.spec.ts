import {PasswordMatchDirective} from './password-match.directive';

describe('PaswordMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMatchDirective();
    expect(directive).toBeTruthy();
  });
});
